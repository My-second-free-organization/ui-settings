spring:
  application:
    name: integration-salesforce
salesforce:
  client-id: ${SALESFORCE_CLIENT_ID}
  client-secret: ${SALESFORCE_CLIENT_SECRET}
  instance-url: ${SALESFORCE_INSTANCE_URL:https://login.salesforce.com}
